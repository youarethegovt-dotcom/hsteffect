[functions]
  # This tells Netlify how to handle the heavy image math
  external_node_modules = ["sharp"]
  node_bundler = "esbuild"

[functions."generate-diagram"]
  # This gives us the 26-second window for the AI to draw
  timeout = 26
